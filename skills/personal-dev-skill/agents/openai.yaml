interface:
  display_name: "PersonalDevSkill"
  short_description: "Low-context delivery from requirements to live tests"
  default_prompt: "Use $personal-dev-skill to deliver this work through the requested endpoint with verified evidence."
policy:
  allow_implicit_invocation: true
